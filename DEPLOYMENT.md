# 🚀 GitHub Pages 部署指南

## 📋 前置步骤

要启用 GitHub Pages 并自动部署网页版数据库，请按以下步骤操作：

### 1️⃣ 合并 Scaffold PR

首先，需要将 `scaffold/frontend-backend-structure` 分支合并到 `main` 分支：

1. 访问您的 GitHub 仓库页面
2. 进入 "Pull requests" 标签
3. 找到标题为 "Scaffold: frontend/backend structure for 7-element alloy database" 的 PR
4. 点击 "Merge pull request" 按钮
5. 确认合并

### 2️⃣ 启用 GitHub Pages

合并 PR 后，需要在仓库设置中启用 GitHub Pages：

1. 进入仓库的 **Settings** (设置)
2. 在左侧菜单中找到 **Pages** (页面)
3. 在 "Source" (源) 部分，选择 **GitHub Actions**
4. 点击 **Save** (保存)

### 3️⃣ 触发部署

GitHub Pages 配置完成后，部署会自动开始：

- 方式1：合并 PR 时会自动触发部署
- 方式2：在 Actions 标签页手动运行 "Deploy to GitHub Pages" workflow

### 4️⃣ 访问您的数据库网页

部署完成后（通常需要 2-5 分钟），您可以通过以下 URL 访问：

```
https://wqchen007.github.io/jkw-7element-alloy-database/
```

## ✅ 验证部署

1. 进入仓库的 **Actions** 标签
2. 查看 "Deploy to GitHub Pages" workflow 的运行状态
3. 等待状态变为绿色的 ✓（成功）
4. 访问上述 URL 查看您的数据库网页

## 🎨 网页功能

部署后的网页将提供：

- 🔍 **实时搜索**：输入元素符号或材料名称即时搜索
- 📊 **数据展示**：以表格形式展示所有材料数据
- 📱 **响应式设计**：支持电脑、平板、手机访问
- 🌏 **中文界面**：完全中文化的用户界面
- 💾 **离线数据**：数据直接加载，无需后端服务器

## 🔄 后续更新

每次向 `main` 分支推送更新时，GitHub Pages 会自动重新部署：

```bash
# 本地更新数据或代码后
git add .
git commit -m "Update materials data"
git push origin main

# 等待 2-5 分钟后，网页会自动更新
```

## ⚙️ 自定义域名（可选）

如果您有自己的域名，可以：

1. 在 Settings > Pages 中配置 Custom domain
2. 在域名提供商处添加 CNAME 记录指向 `wqchen007.github.io`
3. 等待 DNS 生效后即可使用自定义域名访问

## 🆘 常见问题

### Q: 部署失败怎么办？

A: 检查 Actions 标签页中的错误日志，常见问题：
- 确保已启用 GitHub Pages
- 检查 workflow 权限设置
- 确认 `scripts/generate-sample-data.js` 可以正常运行

### Q: 页面显示 404？

A: 可能原因：
- GitHub Pages 还未启用
- 部署尚未完成（等待几分钟）
- URL 拼写错误

### Q: 如何更新数据？

A: 
1. 修改 `backend/data/materials.json` 文件
2. 或运行 `scripts/generate-sample-data.js` 生成新数据
3. 提交并推送到 main 分支
4. 等待自动部署完成

## 📚 相关文档

- [GitHub Pages 官方文档](https://docs.github.com/en/pages)
- [GitHub Actions 文档](https://docs.github.com/en/actions)
- [项目 API 文档](./docs/API.md)

---

**下一步**: 合并 PR 并启用 GitHub Pages，让您的数据库在线可访问！ 🎉
